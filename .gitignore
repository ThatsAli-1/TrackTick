# Minimal ignore: large deps and bulky generated output only.

# secrets & local env (never push)
.env.local
.env*.local

# legacy SQLite (replaced by MongoDB)
local.db
local.db-shm
local.db-wal

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# bulky generated / caches
/coverage
/.next/
/out/
/build
